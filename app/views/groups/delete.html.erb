<%= link_to("Back to list", { :action => 'index'}, :class => 'back-link') %>

<div class="groups delete">
  <h2> Delete Group</h2>

  <%= form_for(:group, :url => { :action => 'destroy', :id => @group.id }) do |f| %>
    <p>Are you sure you want to permanently delete this group?</p>

    <p class="reference-name"><%= @group.id %> <%= @group.group_name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Group") %>
    </div>

  <% end %>

</div>
